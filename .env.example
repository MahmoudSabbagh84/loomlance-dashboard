VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_PUBLIC_SITE_URL=http://localhost:5173
VITE_SPLASH_URL=https://splash.loomlance.com

# Integration providers: 'mock' (default — no external accounts) or the real provider.
# Flip to real ONLY after deploying the Edge Functions + setting their secrets. See pre-production.md.
VITE_EMAIL_PROVIDER=mock
VITE_PAYMENTS_PROVIDER=mock
