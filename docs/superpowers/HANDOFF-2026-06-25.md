# LoomLance — Session Handoff (2026-06-25, Mac → Windows)

Paste the short prompt at the very bottom into a fresh Claude Code session on the **Windows** machine.
This file is the full context it will read. (Supersedes `HANDOFF-2026-06-24.md`, which was Windows → Mac.)

---

## 0. TL;DR — what changed today (Mac session)

- Ran the **fraud & security audit** (chose **Option 2 — focused manual pass**) and **live-verified** it
  via the Supabase MCP. **No new P0/P1.** Full report: `docs/superpowers/2026-06-25-security-audit.md`.
- Implemented the **two P2 fixes** the audit found (backend only): contact-form rate limit + declarative
  `verify_jwt`. Committed, **pushed**, **not yet deployed**.
- Dual-homed the MCP config: added `linear` + `context7` to `.mcp.json` (was machine-local before).
- LOO-92 / LOO-91 implementation **not started** — they're UI-heavy and need Impeccable (which lives on
  *this* Windows box already). They are the main queued work.

## 1. Repos — pull these first on Windows

| Repo | GitHub | Branch / HEAD (after today's push) |
|---|---|---|
| **Dashboard** | `MahmoudSabbagh84/loomlance-dashboard` | `main` @ **`8fec8ed`** |
| **Splash** | `MahmoudSabbagh84/loomlance-splah` *("splah" typo is real)* | `main` @ `779654e` (unchanged today) |

**Resume commands on Windows** (adjust paths to your `C:\Users\mahmo\Desktop\...` layout):
```bash
cd C:\Users\mahmo\Desktop\LoomLance-Dashboard
git checkout main && git pull --ff-only origin main      # expect HEAD = 8fec8ed
npm install                                              # only if package.json changed (it didn't today)

cd C:\Users\mahmo\Desktop\LoomLance-Splash
git checkout main && git pull --ff-only origin main      # expect HEAD = 779654e (no-op today)
npm install
```
Everything from the Mac session is committed + pushed; nothing is stranded.

## 2. Today's commits (dashboard, already on origin/main)

- `d989eaf` fix(security): contact-form rate limit + declarative verify_jwt; audit report
- `8fec8ed` chore(mcp): add linear + context7 servers to .mcp.json

## 3. Tooling on Windows (should already be present — this is the origin machine)

- **Superpowers plugin** + **Impeccable skill** (`~/.claude/skills/impeccable`) — these *originated* on
  Windows, so they should already be installed. Confirm with a quick `/impeccable` and that
  `brainstorming`/`writing-plans`/`executing-plans` skills resolve. (On the Mac they were **missing**,
  which is why LOO-92/91 stayed blocked there.)
- **MCPs** — `.mcp.json` now declares **supabase + linear + context7** (committed today). Each needs its
  own auth/trust on first load: `supabase` (OAuth, project `zbipqfsqxnvrzhpdjvvy`), `linear` (OAuth into
  team **LOO**), `context7` (trust, no creds). Use `/mcp` to authenticate.
- **Memory** is machine-local and does NOT transfer; key facts are in this doc + Linear.

## 4. Project conventions (unchanged — follow these)

- **Dual-home everything:** Linear (team LOO, "Go-Live Readiness") **and** local (`docs/` + memory).
- **Push is manual/gated** — the user pushes; don't push unless asked (exception: a handoff like this).
- **All UX/UI via Impeccable.** Match the "Slate Pro" design tokens. Never hand-roll UI.
- **Commit trailer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- User runs the app LIVE during sessions — scope cleanup to your own markers; never bulk-delete.

## 5. Security audit — result (full detail in the 2026-06-25 report)

Data layer + money path are well-hardened. Tenant isolation airtight (all tables RLS `user_id =
auth.uid()`), Stripe webhooks signature-verified + idempotent, amounts recomputed server-side,
self-upgrade blocked by triggers. **Live-verified:** `mock_payments_enabled = false` in prod,
`tables_without_rls = 0`, every `SECURITY DEFINER` RPC gates on `auth.uid()`.

- **P2 (FIXED, committed `d989eaf`, NOT deployed):** contact-form per-IP rate limit (5/10 min) + pinned
  `verify_jwt` for all 8 edge functions.
- **Owner-actions still open:** enable leaked-password protection (Auth → Policies); **drop**
  `mock_pay_invoice` before prod (disabled live, but still exists/anon-executable → keep LOO-5 open).

## 6. WHERE WE LEFT OFF — pick up here (priority order)

1. **Deploy the P2 fixes** (committed, not yet applied to the remote project):
   ```bash
   supabase db push                              # applies 20260625000000_contact_rate_limit.sql
   supabase functions deploy contact-form        # now reads verify_jwt from config.toml
   # optional: redeploy the other 7 functions so the gateway picks up their pinned verify_jwt
   ```
   Smoke: 6 rapid contact submits from one IP → the 6th returns HTTP 429.
2. **Create the 4 Linear issues** (drafted below; Linear MCP wasn't auth'd on the Mac):
   - [Done] contact-form rate limit (P2) — record commit `d989eaf`.
   - [Done] declarative `verify_jwt` (P2) — record commit `d989eaf`.
   - [Todo] enable leaked-password protection + bump min password length 6→8 (P3, owner-action).
   - [Update **LOO-5**] mock_pay_invoice verified off live; **drop the function before prod**.
3. **LOO-92 — Onboarding + Business page** (spec: `docs/superpowers/specs/2026-06-24-onboarding-business-design.md`).
   Now UNBLOCKED on Windows (Impeccable present). Flow: read spec → `writing-plans` → `executing-plans`.
   `business_type` toggle (Business name / Your name) + tier-aware business-first checklist + one-time welcome.
4. **LOO-91 — Payments + invoice pay-flow** (spec: `docs/superpowers/specs/2026-06-24-payments-payflow-design.md`).
   Drop the master "Accept online payments" toggle (verified 0 accounts affected); connected = offered per
   provider; pay page = explicit client method choice. read spec → `writing-plans` → `executing-plans`.

## 7. Still pending from before (not regressions)

- **Paid-signup smoke test** (Stripe **test mode**) to verify LOO-89 live — never run.
- **Go-live owner-actions:** LOO-8 (Stripe live keys), LOO-9 (prod Auth redirect URLs), LOO-10 (IAM SES
  key), LOO-11 (token rotation), LOO-13 (register Stripe events: `async_payment_failed`,
  `charge.refunded`, `charge.dispute.created`), LOO-90 (Stripe portal config), LOO-5 (drop mock-pay).

## 8. ⬇️ PASTE THIS INTO CLAUDE CODE ON WINDOWS

> I'm resuming LoomLance on my Windows machine. Pull both repos — dashboard
> `github.com/MahmoudSabbagh84/loomlance-dashboard` (expect `main` @ `8fec8ed`) and splash
> `github.com/MahmoudSabbagh84/loomlance-splah` (`main` @ `779654e`) — `npm install` if needed. Confirm
> the **superpowers** plugin + **impeccable** skill are present (they live on this machine) and
> authenticate the **supabase + linear (team LOO) + context7** MCPs via `/mcp`. Then read
> `docs/superpowers/HANDOFF-2026-06-25.md` end-to-end. Pick up at section 6: (1) deploy the committed P2
> security fixes (`supabase db push` + `supabase functions deploy contact-form`), (2) create the 4 Linear
> issues, then (3) start **LOO-92** (onboarding/business) and **LOO-91** (payments) from their specs in
> `docs/superpowers/specs/` via writing-plans → executing-plans. Follow the project conventions
> (dual-home Linear+local, manual push, all UI via Impeccable, the commit trailer).
