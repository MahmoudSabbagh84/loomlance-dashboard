# LoomLance — Session Handoff (2026-06-24, Windows → Mac)

Paste the short prompt at the very bottom into a fresh Claude Code session on the Mac. This file is the full context it will read.

---

## 1. Repos (both fully pushed — pull these on the Mac)

| Repo | GitHub | Local (Windows) | Branch / HEAD |
|---|---|---|---|
| **Dashboard** (app.loomlance.com — React+Vite SPA) | `MahmoudSabbagh84/loomlance-dashboard` | `C:\Users\mahmo\Desktop\LoomLance-Dashboard` | `main` @ `3b959eb` |
| **Splash** (loomlance.com — static HTML/Tailwind) | `MahmoudSabbagh84/loomlance-splah` *(note: "splah" typo in the repo name is real)* | `C:\Users\mahmo\Desktop\LoomLance-Splash` | `main` @ `779654e` |

On the Mac: `git clone` both, then `npm install` in each. Everything is committed + pushed; nothing is stranded on Windows.

## 2. Tooling that was active this session (install/configure on the Mac)

- **Superpowers plugin** (claude-plugins-official). Skills used constantly: `brainstorming`, `writing-plans`, `executing-plans`, `systematic-debugging`. The whole workflow is **brainstorm → spec → writing-plans → executing-plans**.
- **Impeccable skill** (`~/.claude/skills/impeccable`) — ALL UX/UI work routes through it (`/impeccable craft|polish|clarify|...`). It also installs a design hook that scans edited UI files. **Project rule: never hand-roll UI.**
- **MCP servers** in use: **supabase** (project ref `zbipqfsqxnvrzhpdjvvy`), **linear-server** (team `LOO`), **context7** (library docs), Google Drive (claude.ai). Configure the same MCPs on the Mac.
- **Memory** lives at `~/.claude/projects/<project>/memory/` (machine-local — won't transfer). Key facts are duplicated in this doc and in Linear, so you're not dependent on it.

## 3. Project context (the essentials)

- **LoomLance** = SaaS for tech freelancers. Tiers: **Solo (free)**, **Freelancer (tier_1)**, **Studio (tier_2)**. Backend: Supabase (Postgres + RLS + RPC + Edge Functions, Deno). Payments: **Stripe** (Connect for invoices + subscription checkout w/ 14-day trial). Email: **AWS SES** via edge functions, sending domain `send.loomlance.com` (DNS in Route53; mailbox on Namecheap PrivateEmail). Both sites deploy on **AWS Amplify**.
- **Splash owns** signup + Stripe Checkout; **Dashboard owns** login. Auth shared via the same Supabase project (splash hands the session to the dashboard via URL-hash, consumed by `detectSessionInUrl`).
- **Working conventions (important):**
  - **Dual-home everything**: Linear (team LOO, "Go-Live Readiness" project) **and** local (`docs/` + memory). Keep in sync.
  - **Push is manual/gated** — the user pushes; don't push unless asked (exception: an explicit handoff like this).
  - **All UX/UI via Impeccable.** Match the "Slate Pro" design tokens.
  - **Commit trailer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
  - **User runs the app LIVE during sessions** — scope any cleanup to your own markers (ZZ/INV-90xx); never bulk-delete.

## 4. What got done this session

- **Email deliverability** (earlier): full SES/DNS auth hardening → 10/10 mail-tester.
- **~20 Go-Live Readiness issues cleared**: payments-path hardening, DB security/perf (RLS FK indexes, search_path, etc.), a11y, **CORS allowlist (LOO-21)**.
- **Contact-form email delay** → root-caused via systematic-debugging as **Namecheap PrivateEmail greylisting** (function returns in 83 ms; delay is receiver-side). Logged **LOO-88** (monitor; chose to wait — SPF/DKIM/DMARC now aligned should reduce it). Not a code bug.
- **LOO-17** (auth-token URL scrub) — verified clean (BrowserRouter + supabase-js v2 auto-scrub). Done.
- **4 QA notes triaged →** LOO-89/90/91/92.
- **LOO-89 — Paid-signup auto-trial: DONE + SHIPPED** (dashboard + splash pushed). Spec + plan in `docs/superpowers/`. Auto-redirects paid signups to Stripe Checkout (14-day trial) on first dashboard load; bail → resume banner; `stripe_customer_id` is the "already-offered" marker (no migration). 141 unit tests green. **Pending: your manual smoke test in Stripe test mode.**
- **LOO-92 — Onboarding + Business page: SPEC written** (`docs/superpowers/specs/2026-06-24-onboarding-business-design.md`). `business_type` toggle (Business name / Your name) + tier-aware business-first checklist + one-time welcome. **Implementation deferred.**
- **LOO-91 — Payments + invoice pay-flow: SPEC written** (`docs/superpowers/specs/2026-06-24-payments-payflow-design.md`). Drop the master "Accept online payments" toggle (verified 0 accounts affected); **connected = offered** per provider; pay page = explicit client method choice. **Implementation deferred.**
- **LOO-90 — Stripe billing portal**: owner-action (configure branding/features in the Stripe Dashboard; no code).

## 5. WHERE WE LEFT OFF — pick up here

**Immediate open decision: the FRAUD & SECURITY AUDIT / stress test.** The user asked for it; I scoped a threat model (RLS tenant isolation, payment fraud incl. the `mock_pay_invoice` public dev-write LOO-5, **server-side tier-limit enforcement**, trial abuse, auth/public edge fns, stored XSS on the invoice/PDF surfaces, rate-limiting, secrets, splash). I offered two ways to run it and **the user has not yet chosen**:
- **Option 1 — multi-agent stress-test workflow** (fan out per surface → adversarially verify → prioritized P0–P3 report). Thorough; spawns ~a dozen+ agents; ~1M+ tokens; **needs explicit opt-in.**
- **Option 2 — focused manual pass** (highest-risk surfaces inline, cheaper).
👉 **Resume by asking the user which option, then run it.** Fold in / verify the known go-live security items rather than re-finding them.

**Queued (await user's go-ahead per spec, then `writing-plans` → `executing-plans`):**
- LOO-92 onboarding/business spec → plan → implement.
- LOO-91 payments/pay-flow spec → plan → implement.

**Owner-actions (the user's, not code) — gating go-live:** LOO-5 (disable `mock_pay_invoice` / `app_config.mock_payments_enabled` before prod), LOO-8 (Stripe live keys), LOO-9 (prod Auth redirect URLs), LOO-10 (IAM SES key), LOO-11 (token rotation), LOO-13 (register new Stripe webhook events: `async_payment_failed`, `charge.refunded`, `charge.dispute.created`), LOO-90 (Stripe portal config).

## 6. ⬇️ PASTE THIS INTO CLAUDE CODE ON THE MAC

> I'm continuing LoomLance work from another machine. Clone (or pull) both repos — dashboard `github.com/MahmoudSabbagh84/loomlance-dashboard` and splash `github.com/MahmoudSabbagh84/loomlance-splah` — and `npm install` each. Make sure the **superpowers** plugin, the **impeccable** skill, and the **supabase + linear (team LOO) + context7** MCPs are configured. Then read `docs/superpowers/HANDOFF-2026-06-24.md` in the dashboard repo end-to-end — it has full session context. We left off at an open decision: the **fraud & security audit / stress test** (Option 1 multi-agent workflow vs Option 2 focused manual). Ask me which option, then proceed. Also queued: implementing the LOO-92 (onboarding/business) and LOO-91 (payments) specs in `docs/superpowers/specs/`. Follow the project conventions in the handoff (dual-home Linear+local, manual push, all UI via Impeccable, the commit trailer).
